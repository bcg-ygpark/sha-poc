# ------------------------------------------------------------
# 기본 정책: 모든 텍스트 파일을 LF로 통일
# (eol=lf는 core.autocrlf보다 우선. Windows에서도 LF로 체크아웃됨)
# ------------------------------------------------------------
* text=auto eol=lf

# ------------------------------------------------------------
# Windows 전용/호환 고려: CRLF가 편한 파일은 예외 허용
# 팀 합의로 LF 유지하고 싶으면 아래 두 줄 지우세요.
# ------------------------------------------------------------
*.bat text eol=crlf
*.cmd text eol=crlf
